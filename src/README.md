# Resumes

To validate all resumes at once:

```bash
% bun validate:resume
```

`validate:resume` uses a json-schema ([jsonresume/resume-schema](https://github.com/jsonresume/resume-schema)) and [ajv](https://ajv.js.org/) to validate.
